{
  "name": "file-browser-api",
  "version": "1.0.0",
  "description": "Single-server Node.js app with file REST API and vanilla SPA client",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
